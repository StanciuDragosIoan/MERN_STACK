/*
    Connecting to mongoDB with mongoose

        copied connection string from clusters->connect->connect ur app 
        created a 'config' directory in the root

        inside 'config' created a default.json file (this is used by the config 
        dependency and inside default.json we can crete global vars to use throughout 
        the app)

        added the connection string as a mongoURI entry in the default.json file

        created a db.js inside the 'config' directory

*/